
{
  "name": "elementor-design-pro",
  "version": "1.0.0",
  "description": "Design tool for Elementor",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "vite",
    "build": "mkdir -p dist && cp index.html index.tsx metadata.json server.js package.json .env dist/ 2>/dev/null || echo 'Copying...' && cp -r components lib dist/ 2>/dev/null || echo 'Done'",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "@supabase/supabase-js": "^2.39.7",
    "@google/genai": "^1.3.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
